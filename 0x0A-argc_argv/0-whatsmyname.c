#include "main.h"

/**
 * main - print program name
 * @argc: no. of arguments to print
 * @argv: argument vector
 *
 * Return: Always 0 (success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
